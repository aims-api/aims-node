import { MessageResponse } from '../../../src/helpers/types'

export const responseProject: MessageResponse = {
  message: 'Added to project',
}

export const responsePlaylist: MessageResponse = {
  message: 'Added to playlist',
}

export const responseCustomTag: MessageResponse = {
  message: 'Added to custom tag',
}
